# Dashboard

The overview page ("Dashboard 2.0"): headline numbers, graphs and a needs-attention strip, all following one date filter, with the boxes drag-arrangeable per person. It renders on every screen size — phones and portrait tablets get the same content reflowed (2-across cards, self-scrolling tables and chart, ≤700/≤1100 rules), with the Modules tile launcher appended below on ≤900px screens; the old phone-only widget list (tasks/calls/week/month) is gone since the dashboard itself shows all of that now. See `.claude/skills/responsive-check` — the standing rule this set.

| Feature | Status | Description | Code |
|---|---|---|---|
| One reduced dashboard call | shipped | Every card and graph is served by `dashboard_rollup` (migration 0162) in a single database round trip, with every clock edge (today, the window, the comparison window, the touch cutoffs) computed once in TypeScript and passed as parameters — the SQL and its tested TS mirror can never disagree about what day it is. Until the migration is run the page falls back to the same buckets computed from targeted queries (slower, identical numbers). The old page's ~72k-row open-lead scan per load is gone. See `docs/DECISIONS.md` #045. | `supabase/migrations/0162_dashboard_rollup.sql`, `src/lib/data/dashboard-rollup.ts` (+ `.test.ts`), `src/lib/actions/dashboard.ts` |
| Date filter over the whole page | shipped | This month (default) / Last 30 days / This quarter / Last 12 months, plus a custom from–to range — the same `DateRangeFilter` every report uses. Every KPI and period-scoped panel recomputes for the chosen window; deltas compare against the previous period (month-to-date compares to the same span of last month — `prevWindow`, pinned in `date-range.test.ts`). | `src/app/(app)/dashboard-view.tsx`, `src/lib/data/date-range.ts` |
| Needs-attention strip | shipped | Always-live cards regardless of the date filter: overdue tasks, overdue payments (phase math identical to the Payments page), contracts awaiting signature, replies waiting (the sidebar badge's own live count), appointments today. Each card opens the page that itemizes its number — Overdue Tasks opens `/tasks`, whose Overdue section counts the same rows as the card. | `src/app/(app)/dashboard-view.tsx`, `src/app/(app)/tasks/` |
| KPI row with change vs. last period | shipped | New leads, appointments, contracts signed $ (with 12-month sparkline), cash collected $ (sparkline), win rate as two rates in one card — signed contracts out of the window's leads, and out of the window's leads that got an appointment set (`winRates` in `win-rates.ts`, shared with the Marketing Analytics tile; same cohort and signed count, so the appointment rate is always the higher; a zero denominator shows an em dash). Every card clicks through. | `src/app/(app)/dashboard-view.tsx`, `src/components/charts/sparkline.tsx` |
| Sales vs. cash collected graph | shipped | 12 months of signed-contract dollars (bars) against collected payments (line) on one axis, with per-month hover/focus tooltips and a screen-reader table twin. A sale is a signed true contract — change orders excluded, the Marketing Analytics rule; collected is succeeded portal payments, manual and Stripe alike. | `src/components/charts/monthly-money.tsx`, `src/lib/charts/scale.ts` (+ `.test.ts`) |
| Funnel, stages, sources, aging, team, calls, production panels | shipped | Sales funnel (the window's cohort: leads → appointment → estimate → signed, with step conversion); pipeline value by stage with a "worked in last 30/60/90 days / all" dropdown (DECISIONS #046); leads by source with signed-dollar credit; money-to-collect aging (not-yet-due / 1–30 / 31–60 / 60+ days late); team table (a sale credited to the contract's Sales team seats, `sale-credit.ts`, DECISIONS #057; migration 0167 re-creates `dashboard_rollup` with that rule) linking to each rep's report on the same window; call activity with a 14-day dial strip; production counts. Money panels (sales-vs-cash, aging, cash KPI, overdue card) render only for `canViewFinancials` — the Payments gate. | `src/app/(app)/dashboard-view.tsx`, `src/components/charts/hbar-rows.tsx` |
| Drag-to-arrange boxes | shipped | Every panel below the KPI row drags by its header to a new spot; the order saves to `profiles.dashboard_panel_order` (own row only, same pattern as the estimates funnel cards / DECISIONS #010) with this browser's localStorage as the fallback and pre-migration path. Orders survive releases: new panels appear at their shipped spot, removed ones vanish quietly (`mergePanelOrder`, pinned by tests). | `src/lib/data/dashboard-layout.ts` (+ `.test.ts`), `src/app/(app)/dashboard-order-prefs.ts`, `src/lib/actions/dashboard.ts` (`saveDashboardPanelOrder`) |

## Dispatch Dashboard

The desk's own overview at `/dispatch-dashboard`, its own top-level sidebar link right under Dashboard (so Settings › Menu Order can place it; it is not a row inside the Dispatch group) and on by default for the Dispatch role (Role Visibility can give it to anyone). It answers one question — is every lead being worked fast, and is the calendar filling — so it carries speed and the board, never money.

| Feature | Status | Description | Code |
|---|---|---|---|
| One reduced dispatch call | shipped | Every card is served by `dispatch_rollup` (migration 0171) in one database round trip, with every clock edge (today, the window, the previous window, the week strip's end, the 7-day untouched / 14-day results / 90-day waiting floors, and the real instant for ages) computed once in TypeScript and passed as parameters. Until the migration is run the page falls back to targeted queries reduced by the same tested builder — slower, identical numbers. `security invoker`, so a dispatcher who is not a supervisor sees only their own leads and the unclaimed pool. See DECISIONS #070. | `supabase/migrations/0171_dispatch_rollup.sql`, `src/lib/data/dispatch-rollup.ts` (+ `.test.ts`), `src/lib/actions/dispatch-dashboard.ts` |
| Needs-attention strip | shipped | Always live regardless of the date filter: new leads nobody has called, texted or noted (pre-appointment stage, last 7 days) with the oldest one's age; replies waiting (the sidebar badge's own count); overdue follow-ups; today's appointments the customer has not confirmed; past appointments (last 14 days) still without a result; the unclaimed pool. Each card opens the page that itemizes it. | `src/app/(app)/dispatch-dashboard/dispatch-dashboard-view.tsx` |
| Pace KPIs with change vs. last period | shipped | Today / Last 7 days (default) / This month / Last 30 days / custom. New leads; reached within 1 hour (a lead's first touch — earliest call logged, customer text sent or note — within 60 minutes of arriving, with the median minutes to first touch); appointments booked (created in the window) with the booking rate out of new leads; show rate (Showed or Won out of appointments dated in the window with any result); dials with connect rate and outbound texts. Rates move in points, counts in percent. | same |
| Today's board, next 7 days, waiting by age, call outcomes, the desk | shipped | Today's non-cancelled visits by time with one pill each (Showed / No-show / No rep assigned / Awaiting customer / Rep not confirmed / Confirmed); seven day-columns of booked visits from today; pre-appointment leads of the last 90 days bucketed under 1 / 1–3 / 4–7 / 8–14 / over 14 days; call outcomes by the company's own disposition names (the empty one left out); and one row per dispatcher — leads received, leads still waiting, dials, connected, booked (by `events.created_by`), showed, book rate — plus the unclaimed pool as a row. Texts have no sender column, so the desk carries no per-person text count. | same, `src/app/globals.css` (`.dd-*`) |

