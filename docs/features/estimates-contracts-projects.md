# Estimates, Contracts & Projects

The document lifecycle (estimate → signed contract → change orders → completion) and the production/project tracking that follows a signed job.

| Feature | Status | Description | Code |
|---|---|---|---|
| Estimate builder | shipped | Line-item estimate editor (qty/unit/price, margin, discount) with scope sections, locking once signed, and a "recall" flow for post-signature edits. Access-gated with an explicit "you don't have access" message rather than a silent empty list, so missing permissions don't read as missing data. | `src/app/(app)/estimates/`, `estimate-builder.tsx`, `scope-editor.tsx`, `sections-bar.tsx` |
| Send panel: real To/Cc/Bcc, editable message, no channel picker | shipped | A "Send Estimate" button (opening a drawer) plus a "More" menu (Mark Sent, Signed on paper) replace the old five-button cluster (Save & Email/Text/Both, Mark Sent, Signed on paper) on both the estimate builder and the completion certificate editor. The drawer sends one real email with genuine `to`/`cc`/`bcc` headers — the lead's own email plus free-typed extras in To, the Second Contact co-owner plus free-typed extras in Cc, free-typed-only in Bcc — so To/Cc recipients see each other's address the way any mail client's Cc does, and Bcc stays hidden. There's no channel picker: every send tries email and text and uses whichever the contact has on file. The drawer shows the real Subject (read-only) and Message (editable, prefilled with the actual default copy via `previewEstimateEmail` — no token minted, nothing sent) a rep can rewrite before sending; an untouched message always defers to the server's fresh default computed from the just-saved total, so editing a line item never ships a stale dollar amount (decision #023). Not added as `estimate_signers`; extra recipients are courtesy copies, not signing parties. Malformed addresses are reported back per bucket, never silently dropped. | `src/lib/estimate-recipients.ts` (`resolveEstimateRecipients`), `src/lib/estimate-email-copy.ts` (`defaultEstimateNarrative`, `paragraphsToHtml`), `src/lib/email-env.ts` (`sendEmail` cc/bcc), `src/lib/actions/estimates.ts` (`sendEstimateToCustomer`, `previewEstimateEmail`), `estimate-send-panel.tsx`, `estimate-builder.tsx`, `completion-editor.tsx` |
| Optional line items | shipped | A line marked Optional shows priced on the customer's document with a tick box; it joins the subtotal, tax and deposit only when the customer ticks it in the portal (totals re-derived server-side). An office save clears earlier ticks — same reasoning as the recall flow — and the PDF spells the state out in words. | `estimate-builder.tsx`, `src/components/optional-item-checkbox.tsx`, `src/lib/actions/portal-estimates.ts` (`setOptionalItemAsCustomer`), `supabase/migrations/0142_optional_line_items.sql` |
| AI scope-to-line-items | shipped | Claude proposes priced line items from a scope description; nothing reaches the actual estimate until a human explicitly accepts each line — by design, so "the AI suggested it" is never the justification for a price on a document a homeowner signs. When a call to Claude fails, Generate / Format / price-lines say why in plain words (key rejected, service busy, or the API's own reason such as an empty credit balance) via `aiFailureFromError` — the same wording as the assistant chat — and the full error goes to the logs and Sentry tagged with the company. The model picked in Settings decides the thinking setting (`thinkingFor`): Opus 5 / Sonnet 5 think adaptively, Haiku 4.5 (which rejects adaptive thinking with a 400) runs without. | `generate-lines-modal.tsx`, `src/lib/actions/scope-ai.ts`, `src/lib/ai-failure.ts`, `src/lib/ai-models.ts` |
| Dead portal links self-serve | shipped | A used or expired sign-in link no longer dead-ends: the login page shows why it failed and points at the "email me a sign-in link" form, which now also matches the second contact's email — so on a joint-owner job, the co-owner who opened the shared link second (one email, one link, `docs/DECISIONS.md` #022) gets their own fresh link without calling the office (#030). Enumeration-safe replies and the street-number challenge unchanged. | `src/lib/portal/login-notice.ts`, `src/app/portal/page.tsx`, `portal-login-form.tsx`, `src/lib/actions/portal.ts` (`requestPortalLink`) |
| Portal e-signature | shipped | Customer-facing signing flow (outside this domain's UI, see `src/lib/portal/`) with signature evidence capture and a rep notification once a document is fully signed. | `src/lib/estimate-signing.ts`, `src/lib/portal/signature-evidence.ts`, `src/lib/portal/rep-signed-notification.ts` |
| Signature evidence on the document | shipped | Each e-signature on the document and its PDF shows an evidence line — "Signed Sep 20, 2026, 10:57 AM PDT · IP 146.75.146.1" — from the IP/user-agent/timestamp captured server-side at signing. The contractor's send-time signature records the same evidence. Times print in the company's own clock (`company_profile.timezone` → IANA via `companyIanaZone`), always labelled (PDT / PST), and the "signed 9/20/2026" date beside the party uses the same clock so an evening signature never reads as tomorrow; UTC, labelled, is only the fallback when no zone resolves (`docs/DECISIONS.md` #055). Paper signatures and pre-capture rows show no line rather than fabricated precision. | `src/lib/portal/signature-evidence.ts` (`signatureEvidenceLine`, `signedOnLabel`), `src/components/estimate-document.tsx`, `src/lib/pdf/document-pdf.ts`, `src/lib/actions/estimates.ts` (send) |
| Signed-on-paper recording | shipped | Records a pen-signed document: who signed, when (backdatable), optional scan upload — after which it behaves identically to a portal-signed document. Nothing is sent to the customer for this flow. | `signed-on-paper-dialog.tsx`, `src/lib/actions/estimates.ts` (`markSignedOnPaper`) |
| Estimate approval workflow | shipped | Optional mandatory-approval gate before an estimate can be sent; the on/off switch lives on the same screen that lists what's pending, so the decision is made next to its consequence. The screen now has a sidebar link ("Estimate Approvals", Admins only, appended beside Admin Settings since it has no Role Visibility cell) — before that it was reachable only by typing its URL, which is how the switch went unfound. A per-person **Send Without Approval** switch in Users & Roles (0179, Admin-only to change, enforced by a trigger too) lets chosen people, closers typically, skip the wait: their send approves the document in their name and notes it on the contact (`docs/DECISIONS.md` #081). The gate is asked in the app before anything leaves (`approvalOnSend`, `src/lib/estimate-approval-gate.ts`): the email/text send and the signed-on-paper path refuse an unapproved document before a message goes out or a signer row is written, and the estimate page hides the send controls behind a hold note that says who approves. The trigger (0136) stays as the backstop, and the send's status write is now checked — a refusal is reported instead of leaving an emailed document in Draft with a dead link (`docs/DECISIONS.md` #054). A send also replaces any earlier send-time contractor signature rather than adding a second "Contractor" line. Admins meet an **Approve** button wherever they meet the hold — in the document's hold note ("Approve EST-1110 here or on the Estimate Approvals screen") and beside the "Waiting for admin approval" chip on Estimate Status (`ApproveEstimateButton`); approving refreshes in place, so the Send controls appear without a trip to the Approvals screen. The list carries dropdown filters over the columns the eye scans — Customer, Written by, and a value/newest sort — with options built from the pending rows themselves (`approvalFilterOptions`, so approving someone's last draft drops their name from the filter too) and a "N of M waiting" count that follows the filters. | `src/lib/approvals-filters.ts` (+ `.test.ts`), `src/app/(app)/estimate-approvals/`, `src/lib/actions/estimate-approval.ts`, `src/lib/estimate-approval-gate.ts` (tested), `src/components/approve-estimate-button.tsx`, `src/lib/actions/estimates.ts`, `src/app/(app)/estimates/[id]/page.tsx`, `src/lib/nav.ts` |
| Estimate Status page | shipped | The rep-side mirror of the admin's Approvals screen, at `/estimate-status` with its own sidebar link ("Estimate Status", beside Estimates & Contracts; Role Visibility managed, Sales sees it by default, and the View Estimates person-switch gates it like the module it reports on). One row per in-flight document with a meaning-colored status chip answering "who is it waiting on": indigo = waiting for admin approval, blue = ready / the closer reviews & sends (named), amber = sent, waiting on the customer, green = signed — the funnel's own amber/green meanings. RLS scopes rows to the viewer's reach; reads are capped at the newest 250 in-flight documents (never a book scan) and signed rows drop off after 30 days. The status judgment is pure and pinned (`estimateFlowStatus`), gates evaluated in real firing order: approval (0136), then the closer's hold (#024), then the customer. An Admin sees an Approve button beside each "Waiting for admin approval" chip — the chip named the blocker and the admin is the blocker, so the row does something. The board carries Closer, Rep 1, Rep 2 (the document's sales seats, Rep 1 with no seat set following whoever holds the lead until signed, then the stamped `assigned_to` — the estimates list's `effectiveEstimateRepId` rule, via `statusBoardRep1Id`) and a Client view column (viewed on / sent, not opened / not sent, plus a staff Preview link that never counts as a customer view), and dropdown filters over Status, Closer, Rep (either seat) and a customer search, options built from the rows on the board (`estimateStatusFilterOptions`), with an "N of M in flight" count. | `src/app/(app)/estimate-status/page.tsx`, `src/app/(app)/estimate-status/estimate-status-view.tsx`, `src/lib/estimate-status-filters.ts` (+ `.test.ts`), `src/lib/estimate-flow-status.ts` (+ `.test.ts`), `src/lib/data/types.ts` (`PAGE_REGISTRY`), `src/lib/nav.ts` |
| Rep in the estimate header | shipped | The estimate page names its rep beside the customer and address ("Rep: Simon Benhamo"), the same person the estimates list, Estimate Status and the customer's copy name (`effectiveEstimateRepId`). Until signed the rep follows the lead, so the header links to the lead card ("from the lead — change it there", returning to the estimate on close) rather than offering its own picker, which would let the document and the lead disagree. Signed or void, it is the rep who sold the job, with no link; pay seats stay in the Sales team panel. | `src/lib/estimate-rep-line.ts` (+ `.test.ts`), `src/app/(app)/estimates/[id]/page.tsx`, `src/app/(app)/estimates/[id]/estimate-builder.tsx` |
| Company clients named for the company | shipped | A Company contact is its company on every surface (estimate header, customer copy and PDF "Prepared for", Estimates/Contracts/Estimate Status/Projects/Payments/P&L/Collect, invoices, completion certificate, commission, alerts, dispatch, new Production job names, the contract's `{{client_name}}`). The person shows as "Contact: …" on the estimate header and "Attn: …" on the customer's copy, and signs "Customer, on behalf of <company>". Greetings stay personal. A guard test blocks hand-built first + last names (DECISIONS #083). | `src/lib/data/client-name.ts` (+ `.test.ts`), `src/lib/client-name-guard.test.ts`, `supabase/migrations/0181_text_alert_client_name.sql` |
| Who voided it, on the banner | shipped | A void document's yellow banner names who voided it beside the day and reason ("Voided on 9/18/2026 by Asher Peretz — reason"), read by `voided_by` from the whole roster. Only a hand void (Admin) sets `voided_by`; a version auto-voided when a newer one is signed names nobody rather than guessing. | `src/lib/void-banner.ts` (+ `.test.ts`), `src/app/(app)/estimates/[id]/page.tsx`, `estimate-builder.tsx` |
| Closer review before send | shipped | On a lead with a closer, reps build and save the estimate but only the closer (or Office/Admin) can take it out of Draft — send, Mark Sent, or signed on paper; completion certificates are exempt. The builder replaces its send buttons with a banner naming the closer. A hold on top of the per-user Estimates switches in Users & Roles, never a replacement — enforced in the server actions, not a trigger, because the send path writes through the service role where `auth.uid()` is null (decision #024). | `src/lib/estimate-closer-gate.ts`, `src/lib/actions/estimates.ts` (`closerHoldError`), `estimate-builder.tsx`, `page.tsx` (estimates/[id]) |
| Payment schedule | shipped | Deposit + phased payment schedule on a signed estimate — even splits or custom redistribution, tracked against the total. | `payment-schedule.tsx`, `src/lib/actions/estimates.ts` (`generateEstimateSchedule`, `saveEstimatePayments`) |
| Per-phase progress billing | shipped | Bills one phase of an already-signed contract at a time (only appears post-signature, since only then is a phase a debt rather than a proposal); phase states (unbilled/billed/clearing/paid/overdue) computed so a deposit never pays down an invoice — this was a deliberate fix (see `docs/DECISIONS.md`). A portal checkout the customer opened but never finished (pending, with a Stripe session and no payment intent — `isUnfinishedCheckout`) does not count as clearing, so backing out of Stripe's page leaves the Pay button in place; clicking Pay again hands back the still-open session (`leftoverCheckoutAction`) rather than opening a second one, so two tabs can't pay the same phase twice. | `phase-billing.tsx`, `src/lib/actions/progress-billing.ts`, `src/lib/data/types.ts` (`phaseState`, `isUnfinishedCheckout`), `src/lib/stripe/checkout-reuse.ts` |
| Change orders | shipped | Change orders against a signed contract only — the estimate itself stays editable pre-signature, so a change order can't be raised against a document the customer hasn't agreed to yet. | `change-orders.tsx`, `src/lib/actions/change-orders.ts` |
| Change-order billing rollup on the contract | shipped | A signed change order is one mirror row on the parent contract's schedule, but its money is usually collected phase-by-phase on the change order's own schedule — those payments settle the change order's rows, so the mirror row used to read "Not billed" over cash in hand. The rollup reads the change order's own billing back onto the mirror row: Paid when fully covered, "Partially paid — $X of $Y paid on EST-…-CO1" when part has landed, Clearing while an ACH is in flight. While the change order holds any of the row's money, the parent row's Bill/Record buttons are off (both act on the full amount, which would collect twice); a payment recorded directly on the mirror row still wins, since billing a change order in one payment from the parent remains a live flow. Mirror row ↔ change order link is by doc number (see `docs/DECISIONS.md` #015). | `src/lib/data/change-order-rollup.ts`, `payment-schedule.tsx`, `phase-billing.tsx`, `page.tsx` (estimates/[id]) |
| Contract revisions (new version) | shipped | "Create new version" on a signed (or part-signed) contract copies the whole document — lines, sections, terms, schedule, customer signers — into a draft with the same doc number and version + 1 (`supersedes_id` links them), so fixing a few words never means retyping the job. Late-filled contract tokens (`{{contract_total}}` etc.) are restored so the new send fills the new figures. The old version stays in force until the customer signs the new one, at which point it is auto-voided ("Superseded by …") and its unbilled phases cancelled; billed/paid history stays on the old version. Distinct from a change order, which adds to a contract instead of replacing it. | `src/lib/actions/estimate-revisions.ts`, `src/lib/contracts/restore-late-tokens.ts`, `src/lib/estimate-signing.ts`, `estimate-builder.tsx`, `supabase/migrations/0146_estimate_revisions.sql` |
| Funnel cards drag-to-reorder | shipped | The Estimates funnel cards can be dragged into any order. Saved on the profile (`estimate_funnel_order`, migration 0145) so the arrangement follows the login to any device, with the browser's localStorage as fallback and instant cache; a card added in a later release slots in at its default spot without undoing the arrangement (see `docs/DECISIONS.md` #010). Value colors are keyed to meaning (amber awaiting signature, green signed, red lost, grey cancelled) rather than position, so they travel with the card. | `estimates-view.tsx`, `src/lib/data/funnel-order.ts`, `src/lib/actions/funnel-order.ts` |
| Pending change orders card | shipped | A "Change Orders · pending" card on the Estimates funnel: change orders still unsigned and alive (Draft/Sent/Viewed, expiry respected), totalling the money still waiting for a signature. Attached spans every status, so the extras that need chasing were buried among signed and settled documents. | `estimates-view.tsx`, `src/lib/data/pending-change-orders.ts` |
| Estimates list at 79k contacts | shipped | The list page fetches only the leads its documents actually reference (`.in("id", leadIds)`) instead of the company's whole contact book, which at 79k rows froze the browser on every visit; the New Estimate dialog reaches every lead through a debounced server-side search (`searchEstimateLeads`, matching name, company, email and address under RLS) instead of filtering a shipped array. Same cure as Contacts and the pipeline board (`docs/DECISIONS.md` #019/#020); the pages still carrying the whole book are logged in `docs/TECH_DEBT.md`. | `src/app/(app)/estimates/page.tsx`, `new-estimate-dialog.tsx`, `src/lib/actions/lead-search.ts` |
| Project cards follow the filters | shipped | The six money/count cards on Projects recompute over the rep/client/search/date-filtered rows (and the selected status chip), so filtering to one rep shows that rep's Sold/Collected/Net cash — not the company's totals over a one-row table. Chip labels keep whole-bucket counts, same split as Payments. Sums live in `projectTotals` (pure, tested). | `src/app/(app)/projects/projects-view.tsx`, `project-filters.ts` |
| Funnel cards follow the salesperson filter | shipped | Ticking reps in the Salesperson filter re-totals every funnel card to just those reps' documents (count and money; a document with no salesperson drops out, same as the table rows). Before, the cards kept company-wide totals over a filtered table, so "Contracts $770,599" read as the selected rep's number. The selection survives switching cards (only the Status filter clears, since its options are the card's own statuses), so one rep's funnel can be read card by card without re-ticking; a ticked rep stays listed in the dropdown even on a card they have nothing on, so the filter is always visible and undoable. Card membership/totalling rules (kind, effective status, Void excluded from money, Attached totalling only signed) live in one lib the cards and the table share. | `estimates-view.tsx`, `src/lib/data/funnel-cards.ts` |
| Job costs | shipped | Tracks costs against a job/estimate for margin visibility. | `job-costs.tsx` |
| Completion certificate | shipped | One certificate per signed job, closing it out and starting the warranty clock; company-editable template seeded with a default written against this system's own contract (1-yr labour warranty §10, CA lien-release rights §11 / Civil Code 8200) — deliberately not styled as a county-recorded "Notice of Completion", which is a legally different instrument. | `completion-certificate.tsx`, `src/lib/actions/completion.ts`, `src/lib/contracts/completion.ts` |
| Contract template merge fields | shipped | Token-substitution engine (`{{contract_no}}`, `{{project_address}}`, etc.) shared as a plain module between the client-side template editor and the server-side estimate generation, so both describe the same tokens once. | `src/lib/contracts/merge.ts` |
| Record payment | shipped | Manual payment recording against an estimate/phase (for payments taken outside the portal, e.g. check/cash). | `record-payment.tsx` |
| Estimate document PDF | shipped | Generates the printable/downloadable PDF for an estimate/contract. | `src/lib/pdf/document-pdf.ts` |
| Closer sees the whole estimate | shipped | A sales-scoped closer (named on `leads.closer_id`) can read an estimate's line items, signers and payments, not just the estimate row — the two SQL lead-visibility rules (`current_visible_lead_ids`, `lead_visible_to_current_user`) grant the same seats, pinned by a test (see `docs/DECISIONS.md` #009). | `supabase/migrations/0134_lead_closer.sql`, `supabase/migrations/0143_closer_sees_estimate_contents.sql`, `src/lib/data/lead-visibility-rules.test.ts` |
| Estimate team display | shipped | Resolves which rep/dispatcher name appears on a customer-facing estimate — the closer if one exists, else the assigned rep — read with the service role since the customer portal has no authenticated staff session and a colleague's lead may not otherwise be readable to the viewer. | `sales-team.tsx`, `src/lib/estimate-team.ts` |
| Photos on estimate | shipped | Job/estimate photo attachment and gallery. | `photos-panel.tsx` |
| Contract Board | shipped | Production → Contracts: every contract (estimates row, `kind='contract'`) as a card in a five-column board by signature status — Draft / Sent / Viewed / Signed / Closed (declined + expired + voided together, money excluding voids per column head). Stat cards (Awaiting signature $, Signed this calendar month $, Expiring ≤7 days, Avg days sent→signed over 90 days) recompute over the filtered rows and click to scope the board; cards carry attention chips (expires in Nd, no reply 14d+, opened N×, partial signatures) and open the document, with Signed cards linking to the project report. Deliberately no drag between columns — a contract's status moves only by real events (send, customer open, signature), so the board is a read-only view over the same rows Estimates and Projects read (`docs/DECISIONS.md` #049). | `src/app/(app)/contracts/`, `contract-board.ts` (tested) |
| Documents (legacy) | shipped | Redirects to `/estimates` — kept only so old bookmarks/links still resolve after documents and estimates were merged into one module. | `src/app/(app)/documents/page.tsx` |
| Projects board | shipped | Per-project cards rolling up money (receivable by phase, bill remaining) computed once server-side and shared with printable reports, so the screen and the printout can't disagree. | `src/app/(app)/projects/`, `project-data.ts` (`buildProjectCards`) |
| Job Transactions list | shipped | Every dollar on a job, opened under its Projects row: the ▸ beside the title opens it all, and clicking Collected, Owed, Bills unpaid or Spent (or the 🧾 Bills chip) opens it filtered to those lines. One timeline, newest first and grouped by month: **Paid in** (green; customer payments on the contract, change orders and invoices, clearing ones marked but not counted), **Paid out** (red; job costs with their receipts, Bill to client or "✓ Billed on INV-…", ✎ Edit), **Still owed** (amber; billed phases' unpaid remainder, open invoices, unpaid bills). Record payment opens inline on an owed line; + Invoice and + Add bill sit on top. The totals line is the row's own figures, and the lines are built by the row's rules (`jobLedger`, tested to add up to `computeProjectRollup`), so they can't disagree. It replaced the 🧾 Bills pop-up. Open rows and their filter are remembered in the browser. The internal printed report prints the same timeline as "All transactions" in place of its payments and costs tables; the client copy is unchanged. Loaded on open (a server action, not a poll), for Office, Admin and Production. On a phone each line is a card with the amount top-right. | `src/app/(app)/projects/job-ledger.tsx`, `src/lib/data/job-ledger.ts` (+ `.test.ts`), `src/lib/actions/job-ledger.ts`, `src/app/(app)/projects/[id]/report/page.tsx` |
| Job-row chips colored by meaning | shipped | The quick chips on every Projects row (and the crew cards) are colored by what they mean — green is money in (Contract, Change orders), red is money out (the dashed "+ Add bill" action, and Bills — the record), blue the checklist, indigo the Permits & files drawer (renamed from "Permits & contracts" so it can't echo the Contract chip), purple photos, rose the client, slate the report — and sit clustered in that scan order in a wrapping row instead of the old dot-separated text line where five chips shared one teal. One tested map feeds both views so they can't drift (`docs/DECISIONS.md` #034). | `src/lib/job-chips.ts`, `projects-view.tsx`, `crew-view.tsx` |
| Commission paid in project net cash | shipped | Each project row shows a "Commission paid" column — the payouts and advances actually recorded against that job in the payout ledger (0158), cash only, never the projected share — and Net cash subtracts it, on the rows, the cards, the bleeding chip/triage and the printable projects report ("Internal — includes costs"). Dash while nothing has been paid; a ledger payment tied to no job shows only on `/sales-commission`, which keeps the owed-vs-paid picture per rep. The per-job report (`/projects/[id]/report`) deliberately stays commission-free — it has a client view, and pay never prints for a customer. See `docs/DECISIONS.md` #035. | `src/lib/data/commission-payouts.ts` (`paidCommissionByEstimate`), `src/lib/data/types.ts` (`computeProjectRollup`), `project-data.ts`, `projects-view.tsx`, `project-filters.ts`, `report/page.tsx` |
| Bills-unpaid column & net accrual | shipped | Two columns beside the cash ones: "Bills unpaid" (filed against the job, money not yet out — promoted from a note under Spent) and "Net accrual" (net cash − unpaid bills; unpaid projected commission deliberately excluded — an estimate until every expense is in). Everything red keys off the accrual figure: the "Negative net" chip, the triage order, the warning panel and the Net accrual card's urgent state (the Net cash card goes red on cash alone) — so a job drowning in unpaid bills goes red before the cash leaves. The printable projects report prints the same accrual lines, and a "Net accrual" total card sits beside Net cash — it took the "New this month" card's seat (owner's call; that count stays on its chip). See `docs/DECISIONS.md` #036. | `src/lib/data/types.ts` (`netAccrualCents`, `projectTriageOrder`), `projects-view.tsx`, `project-filters.ts`, `report/page.tsx` |
| Project checklist | shipped | Per-project checklist with a company-defined template that can be applied, plus ad-hoc items with due dates and notes. The AI assistant can propose adding steps or checking them off — as approval cards, same as every AI suggestion (adds keep the page's own gate, `canEditChecklists`, and the hand-add dedupe: a step already on the list is skipped, never doubled). | `project-checklist.tsx`, `src/lib/actions/checklists.ts`, `src/lib/checklist-auto.ts`, `src/lib/actions/ai-actions.ts` |
| Crew view | shipped | Field-facing simplified project view (address, status, photos, checklist, rain-alert reading, add-bill) for crew/production roles. | `crew-view.tsx` |
| Project/job photos & receipts | shipped | Job-site photo upload and receipt capture/attachment per project. | `job-photos.tsx`, `job-ledger.tsx` (receipts show on the job's Transactions list), `src/lib/receipts.ts` |
| Deleting job photos + deletion history | shipped | In a job's Photos popup, Office/Admin see a ✕ on every photo (always visible, for phones/iPads) that deletes it everywhere after a plain-words confirm; a Google Drive photo goes to Drive's trash for 30 days instead of being wiped. Office/Admin/Production get "Remove from job" under each filed photo, which only unfiles it back to the customer. A "Deleted photos" fold lists who deleted what and when (Office/Admin only), from the append-only `lead_file_deletions` table (0182) that every `deleteLeadFile` call writes (`docs/DECISIONS.md` #084). | `job-photos.tsx`, `src/lib/actions/lead-files.ts`, `src/lib/data/lead-file-deletions.ts`, `src/lib/files/drive-trash.ts` |
| Job/production board | shipped | A real kanban now: four status columns (Not Started / In Progress / On Hold / Complete) with drag-between-columns to change status (the pipeline board's motion; drop updates status alone, so a drag can't clobber a form edit). Four clickable summary cards — Active, Starting this week (Mon–Sun), Past end date (red), Unassigned (amber) — recompute over the search/crew-filtered rows and click-filter the board; the pure rules live in `src/lib/production-board.ts`, tested. Cards show crew (whole-roster name lookups, so deactivated assignees keep their name), a date line ("Starts Sep 23", "Sep 1 – Sep 16 · 4d over"), and an "Open project" link to the matching signed contract on Projects (`/projects?focus=<estimateId>`, cleared by its chip). Phones keep the chip-row + card list. Jobs are auto-created at contract signature (decision #048) or by the pipeline's convert-to-job, or by hand; the toolbar's "Add signed jobs" backfills a job for every contract signed before auto-create shipped (idempotent — a lead with a job is skipped, so clicking twice doubles nothing). | `src/app/(app)/production/`, `production-board.tsx`, `job-form.tsx`, `src/lib/production-board.ts`, `src/lib/production-job.ts` |
| Printable project reports | shipped | Per-project printable report (internal + client copy) and a filtered multi-project report, both built from the same rolled-up figures as the Projects board. The per-project report's payment schedule prints the contract's own schedule only — a signed change order is its mirror row there, with money collected on the change order's own schedule read through onto it (`src/lib/data/report-schedule.ts`); listing the change orders' own phases too printed each one twice, one copy always "Not yet billed". | `src/app/(app)/projects/report/`, `src/app/(app)/projects/[id]/report/` |
| Company signup provisioning | shipped | New-company signup flow: provisions the company record and sends role invites. | `src/lib/signup/provision.ts`, `src/lib/signup/invites.ts` |
