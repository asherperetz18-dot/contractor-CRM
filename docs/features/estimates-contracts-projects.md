# Estimates, Contracts & Projects

The document lifecycle (estimate → signed contract → change orders → completion) and the production/project tracking that follows a signed job.

| Feature | Status | Description | Code |
|---|---|---|---|
| Estimate builder | shipped | Line-item estimate editor (qty/unit/price, margin, discount) with scope sections, locking once signed, and a "recall" flow for post-signature edits. Access-gated with an explicit "you don't have access" message rather than a silent empty list, so missing permissions don't read as missing data. | `src/app/(app)/estimates/`, `estimate-builder.tsx`, `scope-editor.tsx`, `sections-bar.tsx` |
| AI scope-to-line-items | shipped | Claude proposes priced line items from a scope description; nothing reaches the actual estimate until a human explicitly accepts each line — by design, so "the AI suggested it" is never the justification for a price on a document a homeowner signs. | `generate-lines-modal.tsx`, `src/lib/actions/scope-ai.ts` |
| Portal e-signature | shipped | Customer-facing signing flow (outside this domain's UI, see `src/lib/portal/`) with signature evidence capture and a rep notification once a document is fully signed. | `src/lib/estimate-signing.ts`, `src/lib/portal/signature-evidence.ts`, `src/lib/portal/rep-signed-notification.ts` |
| Signed-on-paper recording | shipped | Records a pen-signed document: who signed, when (backdatable), optional scan upload — after which it behaves identically to a portal-signed document. Nothing is sent to the customer for this flow. | `signed-on-paper-dialog.tsx`, `src/lib/actions/estimates.ts` (`markSignedOnPaper`) |
| Estimate approval workflow | shipped | Optional mandatory-approval gate before an estimate can be sent; the on/off switch lives on the same screen that lists what's pending, so the decision is made next to its consequence. | `src/app/(app)/estimate-approvals/`, `src/lib/actions/estimate-approval.ts` |
| Payment schedule | shipped | Deposit + phased payment schedule on a signed estimate — even splits or custom redistribution, tracked against the total. | `payment-schedule.tsx`, `src/lib/actions/estimates.ts` (`generateEstimateSchedule`, `saveEstimatePayments`) |
| Per-phase progress billing | shipped | Bills one phase of an already-signed contract at a time (only appears post-signature, since only then is a phase a debt rather than a proposal); phase states (unbilled/billed/clearing/paid/overdue) computed so a deposit never pays down an invoice — this was a deliberate fix (see `docs/DECISIONS.md`). | `phase-billing.tsx`, `src/lib/actions/progress-billing.ts`, `src/lib/data/types.ts` (`phaseState`) |
| Change orders | shipped | Change orders against a signed contract only — the estimate itself stays editable pre-signature, so a change order can't be raised against a document the customer hasn't agreed to yet. | `change-orders.tsx`, `src/lib/actions/change-orders.ts` |
| Job costs | shipped | Tracks costs against a job/estimate for margin visibility. | `job-costs.tsx` |
| Completion certificate | shipped | One certificate per signed job, closing it out and starting the warranty clock; company-editable template seeded with a default written against this system's own contract (1-yr labour warranty §10, CA lien-release rights §11 / Civil Code 8200) — deliberately not styled as a county-recorded "Notice of Completion", which is a legally different instrument. | `completion-certificate.tsx`, `src/lib/actions/completion.ts`, `src/lib/contracts/completion.ts` |
| Contract template merge fields | shipped | Token-substitution engine (`{{contract_no}}`, `{{project_address}}`, etc.) shared as a plain module between the client-side template editor and the server-side estimate generation, so both describe the same tokens once. | `src/lib/contracts/merge.ts` |
| Record payment | shipped | Manual payment recording against an estimate/phase (for payments taken outside the portal, e.g. check/cash). | `record-payment.tsx` |
| Estimate document PDF | shipped | Generates the printable/downloadable PDF for an estimate/contract. | `src/lib/pdf/document-pdf.ts` |
| Estimate team display | shipped | Resolves which rep/dispatcher name appears on a customer-facing estimate — the closer if one exists, else the assigned rep — read with the service role since the customer portal has no authenticated staff session and a colleague's lead may not otherwise be readable to the viewer. | `sales-team.tsx`, `src/lib/estimate-team.ts` |
| Photos on estimate | shipped | Job/estimate photo attachment and gallery. | `photos-panel.tsx` |
| Contracts (standalone page) | planned | Placeholder "Coming Soon" page — draft/sent/signed contract tracking as its own module isn't built yet; contract behavior today lives on the Estimates pages above. | `src/app/(app)/contracts/page.tsx` |
| Documents (legacy) | shipped | Redirects to `/estimates` — kept only so old bookmarks/links still resolve after documents and estimates were merged into one module. | `src/app/(app)/documents/page.tsx` |
| Projects board | shipped | Per-project cards rolling up money (receivable by phase, bill remaining) computed once server-side and shared with printable reports, so the screen and the printout can't disagree. | `src/app/(app)/projects/`, `project-data.ts` (`buildProjectCards`) |
| Project checklist | shipped | Per-project checklist with a company-defined template that can be applied, plus ad-hoc items with due dates and notes. | `project-checklist.tsx`, `src/lib/actions/checklists.ts`, `src/lib/checklist-auto.ts` |
| Crew view | shipped | Field-facing simplified project view (address, status, photos, checklist, rain-alert reading, add-bill) for crew/production roles. | `crew-view.tsx` |
| Project/job photos & receipts | shipped | Job-site photo upload and receipt capture/attachment per project. | `job-photos.tsx`, `job-receipts.tsx`, `src/lib/receipts.ts` |
| Job/production board | shipped | Kanban-style production board of jobs by status, assignable to crew. | `src/app/(app)/production/`, `production-board.tsx`, `job-form.tsx` |
| Printable project reports | shipped | Per-project printable report (internal + client copy) and a filtered multi-project report, both built from the same rolled-up figures as the Projects board. | `src/app/(app)/projects/report/`, `src/app/(app)/projects/[id]/report/` |
| Company signup provisioning | shipped | New-company signup flow: provisions the company record and sends role invites. | `src/lib/signup/provision.ts`, `src/lib/signup/invites.ts` |
