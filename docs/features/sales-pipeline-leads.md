# Sales Pipeline & Leads

Everything about a lead as a sales record: the pipeline board, dialing, CSV import, refunds, and the contact list.

| Feature | Status | Description | Code |
|---|---|---|---|
| Tasks page | shipped | Every open lead task in one place at `/tasks`, grouped Overdue → Due Today → Coming Up (overdue oldest-first, grouping pinned by `lead-task-groups.test.ts`). The Overdue section counts exactly what the dashboard's "Overdue tasks" card counts (open tasks due before today, company-wide), because that card opens this page — the pipeline's Follow-ups strip stays scoped to the 1,000 newest open leads (it needs per-lead notes for warnings) and now links here for the complete list. Reads the indexed `lead_tasks` table directly and fetches only the referenced leads by id, never the book; a task whose lead RLS hides is dropped, not shown nameless. Rows open the contact; Office/Admin get a Done button (`completeLeadTask`). Registered in `PAGE_REGISTRY` ("Tasks", Dispatch group), so it sits in the sidebar and Role Visibility; Dispatch sees it by default. | `src/app/(app)/tasks/page.tsx`, `tasks-view.tsx`, `src/lib/data/lead-task-groups.ts` (+ `.test.ts`), `src/lib/actions/leads.ts` (`completeLeadTask`) |
| Follow-up task provenance & assignee | shipped | Every task in the lead panel shows who entered it ("Added by …", read from the `created_by` the table has stamped since 0002; the line is simply dropped for system-created tasks like portal reschedule requests and the no-show cron). A task also always lands on somebody's plate: the Assigned To picker's blank option reads "Me — assign to myself" and the server fills the creator in (`createLeadTask`), and AI-created tasks are assigned to whoever accepted the proposal. Byline naming is pinned by `repBylineName` tests — never "Unnamed" on a byline. | `src/app/(app)/pipeline/tasks-panel.tsx`, `src/lib/data/rep-options.ts` (+ `.test.ts`), `src/lib/actions/leads.ts`, `src/lib/actions/ai-actions.ts` |
| Pipeline board | shipped | Kanban-style board of leads by stage, with per-lead panels for notes, tasks, calls, messages, appointments, files, and view history. Companion tabs render inline rather than as separate pages. Board data is server-windowed (`getPipelineBoardData`: each stage's first 40 cards + exact counts, stat tiles as computed numbers, per-lead panels fetched on card open) — the page once shipped every lead, task, note, and file to the browser, which at 79k contacts took minutes to open. See DECISIONS #020. The contact card's Assigned Rep select carries a one-line explainer (whose customer this is — visibility, leaderboard) to keep it distinct from the per-appointment Assigned To seat, which the appointment window mirrors inside its read-only "Customer's team" line (decision #065). | `src/app/(app)/pipeline/page.tsx`, `pipeline-board.tsx`, `lead-form.tsx`, `notes-timeline.tsx`, `tasks-panel.tsx`, `calls-panel.tsx`, `messages-panel.tsx`, `lead-appointments-panel.tsx`, `lead-files-panel.tsx`, `lead-view-trail.tsx`, `src/lib/actions/pipeline-board.ts`, `src/lib/pipeline-aggregates.ts` |
| Bulk email selected contacts | shipped | Multi-select checkboxes in both the Contacts table and Pipeline board (a "Select" toggle there, since cards are otherwise draggable) let office/sales staff write one subject/message and send it individually to every selected contact with an email on file, via the same Resend pipeline as the portal-link send. One bad address never blocks the rest of the batch; contacts with no email are reported as skipped. Gated by `canEditDispatch` (Office/Sales/Dispatch/Admin), same as editing a lead. | `src/lib/bulk-email.ts`, `src/lib/actions/bulk-email.ts`, `src/components/bulk-email-modal.tsx`, `contacts-table.tsx`, `pipeline-board.tsx` |
| Attention digest | shipped | Per-lead warning badges surfaced on the pipeline (no appointments, no notes, no tasks, stale notes, overdue task by N days) so a rep can see which leads are being neglected at a glance. | `src/app/(app)/pipeline/attention-digest.tsx` |
| CSV/XLSX lead import | shipped | Column-mapping UI (name, phone, email, address, project type, value) reading `.xlsx`/csv client-side via the `xlsx` package, with duplicate detection before bulk insert. Handles spreadsheet scale (70k+ rows): uploads in 2,000-row chunks with live progress and a Resume that continues a failed run where it stopped; duplicates are matched in the browser against a downloaded key set of every existing contact (see decision #017). | `src/app/(app)/pipeline/csv-import-panel.tsx`, `src/lib/import-batching.ts`, `src/lib/actions/leads.ts` (`bulkImportLeads`, `getExistingContactKeys`) |
| Three phone numbers per contact | shipped | `phone2`/`phone3` on the lead (migration 0150): bought cold-call lists often carry up to three numbers for one person. Editable on the contact card (Phone 2 / Phone 3, with Call buttons), mappable in the CSV importer, counted by both duplicate checks, and matched by the caller matcher (TS `phone-match.ts` and the SQL `create_lead_for_unknown_caller`) so a prospect ringing back on any of their numbers isn't minted as a new contact. Inbound SMS matching and the dial queue still read only `phone`/`second_contact_phone` (see TECH_DEBT). | `supabase/migrations/0150_lead_phone2_phone3.sql`, `src/lib/data/phone-match.ts`, `src/app/(app)/pipeline/lead-form.tsx`, `csv-import-panel.tsx`, `src/lib/actions/leads.ts` |
| AI lead-conversation analysis | shipped | On-demand Claude analysis of a lead's message/call history rendered on the contact card; stays visible once generated even if the feature toggle is later switched off, since it's treated as a record rather than a live indicator. | `src/app/(app)/pipeline/lead-analysis-panel.tsx`, `src/lib/actions/lead-analysis.ts`, `src/lib/data/ai-analysis.ts` |
| Assistant voice: talk to it, hear it back | shipped | A mic button on the AI assistant chat: tap it, say the question, and it sends itself the moment you stop talking (the words appear in the input as you speak; typing takes over instantly and stays the path that always works). A 🔊 toggle reads replies aloud, remembered per browser, off by default. Both ride the browser's own speech engines (Web Speech API) — no transcription vendor, no key, no per-minute cost — and a browser without them simply never shows the buttons. Mic failures speak plain words ("blocked — allow it in your browser settings"), never raw error codes; saying nothing isn't an error at all. See `docs/DECISIONS.md` #038. | `src/lib/voice-input.ts`, `src/app/(app)/ai-assistant-button.tsx` |
| AI assistant chat | shipped | The topbar ✨ chat answers questions across the viewer's book — leads, schedule, tasks, and now estimates, projects, money to collect, and the last 30 days of calls — streaming the reply word by word instead of sitting on "Thinking…". Every section obeys the page that owns it: estimate figures require View Estimates, receivables require View Financials, and a viewer without estimate access gets project rows with no dollar figure on them (the crew view's rule); every query runs under the caller's own RLS session, and project money comes from `buildProjectCards`, the same rollup the Projects board prints. Summaries are computed over every row while detail lines stay capped, so a count never comes from a truncated list. Office/Admin can still receive propose-only suggestion cards — move stage / assign / create follow-up tasks, and now add project checklist steps or check steps off — which they approve; the model never executes anything, and checklist targets are re-validated against the company at apply time exactly like lead ids (a hallucinated or already-done step drops out as "skipped"). The context also carries every open checklist step with its id, most urgent first, so "what's left on the Smith job" has an answer. A rep-only viewer (Sales/Field with no desk role) gets a chat scoped to **their own book**: every fetch filtered to records assigned to them — leads and totals, appointments, tasks, documents, checklist steps, calls by rep — project cards reduced to their own jobs, and the context opening with a banner that says so; desk roles (Office, Admin, Dispatch, Call Center, Bookkeeping, Production) keep the whole company (decision #060). See `docs/DECISIONS.md` #037. | `src/app/api/ai-assistant/route.ts`, `src/lib/data/assistant-context.ts`, `src/lib/data/assistant-stream.ts`, `src/app/(app)/ai-assistant-button.tsx` |
| Estimate-driven stage auto-advance | shipped | Sending/signing an estimate can move a lead's pipeline stage automatically (e.g. into "Proposal Sent" or "Won"), because the hand-maintained stage field routinely went stale relative to what actually happened on the estimate. Looks up the company's own (editable, per-company) stage names first and no-ops if the target stage was renamed/deleted. Won't drag a lead backwards past stages like Lost/DNC. | `src/lib/pipeline/advance-stage.ts` |
| Dial queue / sales dialer | shipped | Sequential call-through session over a filtered lead list with disposition logging, mid-call quick-edit of lead fields, inline appointment booking, and note-taking, gated to sales-center-enabled roles. List building is server-paged (`listDialContacts`, 50 rows + total per request) — the page once shipped every lead to the browser to filter there, which at 79k contacts took minutes to open; full Lead rows travel only for a session's selected contacts. See DECISIONS #019. Sessions warn "already called N× today" (rep's calendar day) and pause auto-dial on those contacts; a completed session clears its selection and closing one refreshes the list. The By Lead tab's Call Status (Not Called Yet / Called Before) and Disposition (latest-outbound-call outcome) filters — both judged over outbound calls only — combine with the stage filter to build a fresh-dial or redial list for any pipeline stage. | `src/app/(app)/dial-queue/`, `src/lib/actions/dial-contacts.ts`, `src/lib/dial-filters.ts`, `src/lib/actions/call-logs.ts` |
| Lead refunds | shipped | Tracks refund requests against a lead (Requested/Received/Denied) with age-in-days and CSV export. | `src/app/(app)/lead-refunds/`, `src/lib/actions/leads.ts` (`resolveLeadRefund`) |
| Reply inbox | shipped | Unified SMS conversation view across leads, threading by phone number and distinguishing a customer thread from a teammate thread (carried explicitly, not inferred from the name). Live without its own poll: the popup watcher's 20s alerts poll fires `TEXTS_FRESH_EVENT` once per new inbound text (deduplicated), and the open inbox re-pulls its conversations on it -- a customer's YES lands in the thread without a manual refresh. A compose deep-link pins the target's name and number the moment it is consumed (`replyTargetSnapshot`), because the URL is stripped right after and the next render only carries contacts with message history -- a first-ever text used to lose both and refuse to send. The sidebar badge and the "New text from …" popups come from the same grouping — a conversation is waiting on us when the customer spoke last, over the last 30 days — reduced in the database by `text_alert_rollup` (migration 0168: count, watermark and the five newest popups in one round trip, under the caller's RLS). Until that migration is run the badge falls back to walking the 30-day window per poll through the same tested module — slower, identical numbers (DECISIONS #062). | `src/app/(app)/reply-inbox/`, `src/lib/actions/text-alerts.ts`, `src/lib/data/text-alert-rollup.ts`, `supabase/migrations/0168_text_alert_rollup.sql` |
| Contacts list | shipped | Full contact/lead table view (same underlying lead data as pipeline) for browsing/searching outside the kanban board. Server-paged like the dialer and board (DECISIONS #019/#020): the page carries a 60-row window, counted tiles, and a total; search runs server-side over the whole book; the duplicate banner's grouping (`buildDuplicateGroups`, pure and tested) is computed server-side and loaded after first paint; opening a row fetches that contact's card data; select-all-matching is served and capped at 2,000 recipients. | `src/app/(app)/contacts/` |
| Salespeople leaderboard | shipped | Per-rep leaderboard of leads/value with medal icons for the top performers. The tallies are grouped in the database (`rep_lead_stats`, migration 0156 — one row per rep instead of a scan of every lead); until that migration is run the page falls back to the old slim scan, so it keeps working either way. Sits in the sidebar's Staff group, the people section (not Call Center — it reports on selling, not on calling; not Dispatch — it doesn't work leads; and not Accounting — nothing on it is a company dollar owed). Key, route and Role Visibility overrides unchanged by the move. | `src/app/(app)/salespeople/`, `supabase/migrations/0156_rep_lead_stats.sql`, `src/lib/report-leads.ts`, `src/lib/nav-groups.test.ts` |
| Appointment-setter assignments | shipped | Assigns specific leads/contacts to a setter rep for follow-up, independent of the main pipeline owner. | `src/app/(app)/appt-setter-assignments/`, `src/lib/actions/setter-contacts.ts` |
| Appointment reps see the lead | shipped | A sales-scoped rep booked onto a lead's appointment — either chair, `assigned_to` or `second_assigned_to` — holds the lead like the assigned rep, dispatcher and closer do: the contact in their portal, its estimates/contracts with their contents, and (once seated on the contract) their commission line. Closed the same hole 0134 closed for closers: the second chair could see the visit but not the customer it was for. Both SQL visibility rules updated together, pinned by the same test (decision #009, #024). 0152's inline `events` clause in `leads_select` recursed against the dispatch policy and broke every appointment save — 0159 moved it into `current_appointment_lead_ids()` (security definer, same grant), pinned by the same test file (decision #039). | `supabase/migrations/0152_appointment_reps_see_lead.sql`, `supabase/migrations/0159_fix_appointment_save_recursion.sql`, `src/lib/data/lead-visibility-rules.test.ts` |
| Contract seats keep visibility | shipped | Anyone stamped on a contract's Sales team (`sales_rep_1`/`sales_rep_2`/closer seat) keeps the customer, the contract and their commission line visible after the contact is reseated for a later job — signed the paper, keep the paper. One security definer helper (`current_contract_seat_lead_ids`, the 0159 no-inline-subquery pattern) added to both SQL visibility rules and `leads_select`, pinned by the same test that keeps every seat level. Exists because a returning client's second job with a new team used to hide the first team's own signed contract and the money still owed to them (decision #063). | `supabase/migrations/0169_contract_seats_keep_visibility.sql`, `src/lib/data/lead-visibility-rules.test.ts` |
| Partner rep — two reps, one sale | shipped | `leads.partner_rep_id` (0163): the second salesperson on a partnership job, picked on the contact next to the Closer (the latest appointment's second chair is offered as a one-click suggestion, never auto-set). Both partners hold the lead, the Salespeople grid credits the job to both — each gets the lead and the Won notch, the value split half and half so the grid's total never overstates sales — and at signature the partner takes rep seat two at an even split, adjustable on the Sales Team panel. Deliberately distinct from the closer, who follows the job with their own cut and zero sale credit; the two seats are mutually exclusive. Seat rules mirror the closer's: office/admin or the owning rep change a filled seat, anyone editing the lead fills an empty one, every change lands on the timeline. See decision #050. | `supabase/migrations/0163_lead_partner_rep.sql`, `src/lib/actions/partner-rep.ts`, `src/app/(app)/pipeline/partner-rep-picker.tsx`, `src/lib/report-leads.ts` (+ `.test.ts`), `src/lib/actions/rep-commission.ts` (`getSalesTeam`) |
| Lead sources admin & lead cost by source | shipped | Company-editable list of lead source values (with merge-duplicate-values tool), used on lead create/edit and reporting. Each source carries an optional **Lead cost** box ($; blank = the company default, 0 = free), and the same page edits the company default itself ($375 since 0089, until now reachable only by SQL). A new lead arriving without a cost — pipeline form, CSV import, Facebook, the leads webhook — is priced by the database on insert: its source's figure first, then the company default (migration 0166); leads already priced are never touched. Blank-versus-0 is pinned by `lead-source-cost.test.ts`. This is the per-lead figure each contact carries; the month's actual spend per source lives in the Marketing spend section of the same page (0165) and is what Marketing Analytics prefers. Until 0166 is pasted the page loads with blank boxes and a save names the file to run. See DECISIONS #057. | `src/app/(app)/settings/lead-sources/`, `src/app/(app)/settings/field-options-table.tsx`, `src/lib/data/lead-source-cost.ts` (+ `.test.ts`), `src/lib/actions/lead-field-options.ts`, `supabase/migrations/0166_lead_source_default_cost.sql` |
| Leads webhook (external intake) | shipped | Key-authenticated public endpoint that creates a lead and fires the new-lead SMS alert, through two doors sharing one tested field mapper (`webhook-lead.ts`): a POST (JSON or form-encoded — website forms, Zapier) answering JSON, and a GET with the fields in the query string for dialer web-form buttons (ViciDial appends `phone_number`/`alt_phone`/`first_name`/`address1`/`city`/`state`/`postal_code`/`comments` to the URL it opens; `postal_code` lands in `zip`, `alt_phone` in `phone2`). The GET saves on sight — what a background Dispo Call URL needs — unless the URL carries `&review=1`, which instead shows the agent a script-free check-and-save form pre-filled with the caller's details plus a project-type dropdown from the company's own list, est. value, and notes; Save posts through the same POST door (`respond=html` answers as a page). See DECISIONS #041/#042. The Settings › Incoming Data page prints all the recipes. | `src/app/api/leads/webhook/`, `src/lib/webhook-lead.ts`, `src/app/(app)/settings/incoming-webhooks/` |
| New-lead SMS alert | shipped | Texts the company's configured alert numbers when a new lead comes in, from whatever channel (webhook, CallRail, inbound email). | `src/lib/notify-new-lead.ts` |
| Contact soft-delete / trash & restore | shipped | Deleting a contact is still a real hard delete with cascades, but everything about to be destroyed is snapshotted as JSON into `lead_trash` first (raw `select("*")`, not the typed `Lead` shape, so untyped columns like the Drive folder link aren't silently dropped on restore), and orphaned-but-not-cascaded rows (events, jobs, texts) are recorded by id so a restore can relink them. | `src/lib/lead-trash.ts`, `src/app/(app)/settings/trash/` |
| Per-stage CSV export & delete-all | shipped | Settings → Pipeline Stages shows a live contact count per stage with two actions: "⬇ CSV" streams every contact in the stage as a spreadsheet download (headers match the CSV importer's, so an export re-imports as-is), and "Delete all…" permanently deletes them batch-by-batch with a progress line — built for clearing an imported book (66k+ rows) out of a staging stage. The delete skips the trash (66k snapshots is not a trash, it's a second table) — the confirm says so and points at the CSV as the backup; deletes run as the signed-in user so `leads_delete` RLS has the final word on every batch. See DECISIONS #048. | `src/lib/stage-export.ts` (+ `.test.ts`), `src/app/api/leads/stage-export/route.ts`, `src/app/api/leads/stage-purge/route.ts`, `src/app/(app)/settings/pipeline-stages/` |
