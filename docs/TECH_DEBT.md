# Tech debt

Known shortcuts, deferred work, and things left deliberately unfinished — logged so they're a choice, not a surprise. Format: what/why/impact/where. Remove an entry once it's actually fixed rather than leaving it marked done.

---

_Empty — nothing currently logged. The duplicate `"version"` key in `package.json` that was here is resolved (PR #119); if the repeated-bump pattern across commits recurs, re-log it and flag it to whoever owns the version-bump process._
