import { ComingSoon } from "../coming-soon";

export default function CalendarPage() {
  return (
    <ComingSoon
      title="Calendar"
      subtitle="Month-grid view of appointments linked to leads and jobs"
    />
  );
}
